import {Clock3,CheckCircle2} from "lucide-react";
import {CeoPageHeader} from "@/components/ceo-page-header";
type Props={title:string;description:string;objective:string;features:readonly string[];dependency?:string;status?:"Planejado"|"Em desenvolvimento"|"Em testes"|"Disponível"};
export function ComingSoonModule({title,description,objective,features,dependency,status="Planejado"}:Props){return <div className="mx-auto max-w-5xl space-y-5"><CeoPageHeader title={title} description={description}/><section className="rounded-2xl border bg-white p-6"><div className="flex items-center justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-wide text-blue-600">Roadmap</p><h2 className="mt-1 text-xl font-bold">{objective}</h2></div><span className="rounded-full bg-amber-50 px-3 py-1 text-xs font-bold text-amber-700">{status}</span></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{features.map(f=><div key={f} className="flex gap-3 rounded-xl bg-slate-50 p-4 text-sm"><CheckCircle2 className="size-5 shrink-0 text-blue-600"/>{f}</div>)}</div>{dependency?<p className="mt-5 flex gap-2 rounded-xl border border-blue-100 bg-blue-50 p-4 text-sm text-blue-800"><Clock3 className="size-5 shrink-0"/><span><b>Dependência:</b> {dependency}</span></p>:null}</section></div>}

