"use client";
import {usePathname,useRouter,useSearchParams} from "next/navigation";
export function CeoFilters({units}:{units:Array<{id:string;name:string}>}){const router=useRouter(),path=usePathname(),params=useSearchParams();function set(key:string,value:string){const next=new URLSearchParams(params);next.set(key,value);router.replace(`${path}?${next}`)}return <div className="flex flex-wrap gap-2"><label className="sr-only" htmlFor="ceo-period">Período</label><select id="ceo-period" value={params.get("period")??"today"} onChange={e=>set("period",e.target.value)} className="h-10 rounded-xl border bg-white px-3 text-sm"><option value="today">Hoje</option><option value="7">7 dias</option><option value="30">30 dias</option></select><label className="sr-only" htmlFor="ceo-unit">Unidade</label><select id="ceo-unit" value={params.get("unit")??"all"} onChange={e=>set("unit",e.target.value)} className="h-10 max-w-56 rounded-xl border bg-white px-3 text-sm"><option value="all">Todas as unidades</option>{units.map(u=><option key={u.id} value={u.id}>{u.name}</option>)}</select></div>}

