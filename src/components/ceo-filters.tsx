"use client";
import {usePathname,useRouter,useSearchParams} from "next/navigation";
export function CeoFilters({units}:{units:Array<{id:string;name:string}>}){const router=useRouter(),path=usePathname(),params=useSearchParams();function set(key:string,value:string){const next=new URLSearchParams(params);next.set(key,value);router.replace(`${path}?${next}`)}return <div className="grid w-full gap-2 rounded-2xl border border-slate-200 bg-white/80 p-2 shadow-sm sm:flex sm:w-auto sm:flex-wrap sm:items-center sm:gap-1.5"><label className="sr-only" htmlFor="ceo-period">Período</label><select id="ceo-period" value={params.get("period")??"today"} onChange={e=>set("period",e.target.value)} className="h-10 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm outline-none transition focus:border-blue-400 focus:ring-4 focus:ring-blue-50 sm:w-auto"><option value="today">Hoje</option><option value="7">7 dias</option><option value="30">30 dias</option></select><label className="sr-only" htmlFor="ceo-unit">Unidade</label><select id="ceo-unit" value={params.get("unit")??"all"} onChange={e=>set("unit",e.target.value)} className="h-10 w-full min-w-0 rounded-xl border border-slate-200 bg-white px-3 text-sm outline-none transition focus:border-blue-400 focus:ring-4 focus:ring-blue-50 sm:min-w-48 sm:max-w-56"><option value="all">Todas as unidades</option>{units.map(u=><option key={u.id} value={u.id}>{u.name}</option>)}</select></div>}
