import Link from "next/link";
import { CarFront } from "lucide-react";
import { OperationBadge } from "@/components/operation-badge";
import { formatDateTime,formatDuration,formatMoney,parkingStatus,type ActiveSession } from "@/lib/operator-format";
export function OperatorSessionTable({sessions,timezone,limit}:{sessions:ActiveSession[];timezone:string;limit?:number}){const rows=limit?sessions.slice(0,limit):sessions;if(!rows.length)return <div className="flex items-center justify-center gap-3 p-7 text-sm text-slate-500"><CarFront className="size-6 text-slate-400"/><span>Nenhum veículo estacionado no momento.</span></div>;return <div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-slate-50 text-xs text-slate-500"><tr><th className="px-4 py-3">Placa</th><th>Entrada</th><th>Permanência</th><th>Valor atual</th><th>Status</th><th className="pr-4 text-right">Ação</th></tr></thead><tbody>{rows.map(s=>{const status=parkingStatus(s.status);return <tr key={s.id} className="border-t"><td className="px-4 py-3 font-bold text-blue-600">{s.plate}</td><td>{formatDateTime(s.entered_at,timezone)}</td><td>{formatDuration(s.duration_minutes)}</td><td>{formatMoney(s.amount)}</td><td><OperationBadge {...status}/></td><td className="pr-4 text-right"><Link className="font-semibold text-blue-600 hover:underline" href={`/frentista/saidas?session=${s.id}`}>{s.status==="OPEN"?"Iniciar saída":"Abrir sessão"}</Link></td></tr>})}</tbody></table></div>}

