import Link from "next/link";
import { formatDateTime,formatDuration,formatMoney,type ActiveSession } from "@/lib/operator-data";
const labels:Record<string,string>={OPEN:"Estacionado",PAYMENT_PENDING:"Aguardando pagamento",PAID:"Pago â€” liberar saÃ­da",MANUAL_REVIEW:"RevisÃ£o"};
export function OperatorSessionTable({sessions,timezone,limit}:{sessions:ActiveSession[];timezone:string;limit?:number}){const rows=limit?sessions.slice(0,limit):sessions;return <div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-slate-50 text-xs text-slate-500"><tr><th className="px-4 py-3">Placa</th><th>Entrada</th><th>PermanÃªncia</th><th>Valor atual</th><th>Status</th><th className="pr-4 text-right">AÃ§Ã£o</th></tr></thead><tbody>{rows.map(s=><tr key={s.id} className="border-t"><td className="px-4 py-3 font-bold text-blue-600">{s.plate}</td><td>{formatDateTime(s.entered_at,timezone)}</td><td>{formatDuration(s.duration_minutes)}</td><td>{formatMoney(s.amount)}</td><td><span className="rounded-full bg-blue-50 px-2 py-1 text-xs font-semibold text-blue-700">{labels[s.status]??s.status}</span></td><td className="pr-4 text-right"><Link className="font-semibold text-blue-600 hover:underline" href={`/frentista/saidas?session=${s.id}`}>{s.status==="OPEN"?"Iniciar saÃ­da":"Abrir sessÃ£o"}</Link></td></tr>)}</tbody></table>{rows.length===0&&<p className="p-8 text-center text-sm text-slate-500">Nenhum veÃ­culo no pÃ¡tio.</p>}</div>}
