"use client";
import Link from "next/link"; import { useActionState } from "react"; import { recover, type RecoveryState } from "./actions";
const initial: RecoveryState = {};
export default function ForgotPage() { const [state, action, pending] = useActionState(recover, initial); return <main className="grid min-h-screen place-items-center bg-slate-50 p-4"><form action={action} className="w-full max-w-md space-y-5 rounded-3xl border bg-white p-8 shadow-xl"><h1 className="text-3xl font-bold">Recuperar senha</h1><p className="text-sm text-slate-500">Informe seu e-mail. Por segurança, a resposta é igual mesmo quando a conta não existe.</p><input name="email" type="email" autoComplete="email" required placeholder="voce@exemplo.com" className="h-12 w-full rounded-xl border px-4" />{state.error && <p className="text-sm text-red-600">{state.error}</p>}{state.message && <p role="status" className="rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{state.message}</p>}<button disabled={pending} className="h-12 w-full rounded-xl bg-blue-600 font-bold text-white">Enviar instruções</button><Link href="/login" className="block text-center text-sm font-semibold text-blue-600">Voltar ao login</Link></form></main>; }
