import { DashboardShell } from "@/components/dashboard-shell";import { getOperatorContext } from "@/lib/operator-data";import { operatorNav } from "@/lib/operator-nav";import { createClient } from "@/lib/supabase/server";
export const dynamic="force-dynamic";
export default async function OperatorSettings(){const{access,unitId}=await getOperatorContext();const supabase=await createClient();const[{data:unit},{data:shift}]=await Promise.all([supabase.from("parking_units").select("name,slug,capacity,timezone").eq("id",unitId).single(),supabase.from("cash_shifts").select("id,opened_at").eq("unit_id",unitId).eq("operator_id",access.user.id).eq("status","OPEN").maybeSingle()]);return <DashboardShell nav={operatorNav} active="Configurações" role="Frentista"><div className="mx-auto max-w-4xl space-y-5"><div><h1 className="text-3xl font-bold">Configurações do turno</h1><p className="text-sm text-slate-500">Informações operacionais somente para consulta.</p></div><div className="grid gap-4 md:grid-cols-2"><Info title="Unidade atual" rows={[["Nome",unit?.name??"—"],["Identificador",unit?.slug??"—"],["Capacidade",String(unit?.capacity??0)],["Fuso horário",unit?.timezone??"—"]]}/><Info title="Perfil operacional" rows={[["Operador",access.profile.full_name],["Função","Frentista"],["Caixa",shift?"Aberto":"Fechado"]]}/><Info title="Limites de acesso" rows={[["Tarifas","Somente consulta"],["Usuários","Sem permissão"],["Capacidade","Sem permissão"],["Integrações","Sem permissão"]]}/><Info title="Suporte" rows={[["Sistema","Star Cavalos"],["Orientação","Contate o administrador da unidade"]]}/></div></div></DashboardShell>}
function Info({title,rows}:{title:string;rows:string[][]}){return <section className="rounded-2xl border bg-white p-5"><h2 className="font-bold">{title}</h2><dl className="mt-4 space-y-3">{rows.map(([k,v])=><div key={k} className="flex justify-between gap-3 border-b pb-2 text-sm"><dt className="text-slate-500">{k}</dt><dd className="text-right font-semibold">{v}</dd></div>)}</dl></section>}
