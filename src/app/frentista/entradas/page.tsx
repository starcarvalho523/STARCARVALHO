import { DashboardShell } from "@/components/dashboard-shell";import { EntryForm } from "@/components/entry-form";import { OperatorSessionTable } from "@/components/operator-session-table";import { getOperatorDashboard } from "@/lib/operator-data";import { operatorNav } from "@/lib/operator-nav";
export const dynamic="force-dynamic";
export default async function EntriesPage(){const data=await getOperatorDashboard();return <DashboardShell nav={operatorNav} active="Entradas" role="Frentista"><div className="mx-auto max-w-6xl space-y-5"><div><h1 className="text-3xl font-bold">Registrar entrada</h1><p className="text-sm text-slate-500">A placa, o horÃ¡rio oficial, a tarifa e o operador serÃ£o registrados automaticamente.</p></div><EntryForm carEnabled={data.has_active_car_tariff} motorcycleEnabled={data.has_active_motorcycle_tariff}/><section className="overflow-hidden rounded-2xl border bg-white"><h2 className="border-b p-4 font-bold">Entradas recentes no pÃ¡tio</h2><OperatorSessionTable sessions={data.active_sessions} timezone={data.unit.timezone}/></section></div></DashboardShell>}
