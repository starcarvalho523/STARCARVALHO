import { DashboardShell } from "@/components/dashboard-shell";import { OperatorSessionSearch } from "@/components/operator-session-search";import { getOperatorDashboard } from "@/lib/operator-data";import { operatorNav } from "@/lib/operator-nav";
export const dynamic="force-dynamic";
export default async function VehiclesPage(){const data=await getOperatorDashboard();return <DashboardShell nav={operatorNav} active="Veículos" role="Frentista"><div className="mx-auto max-w-6xl space-y-5"><div><h1 className="text-3xl font-bold">Veículos no pátio</h1><p className="text-sm text-slate-500">{data.vehicles_in_yard} veículos ocupando {data.unit.capacity} vagas.</p></div><section className="overflow-hidden rounded-2xl border bg-white"><OperatorSessionSearch sessions={data.active_sessions} timezone={data.unit.timezone}/></section></div></DashboardShell>}
