import Link from "next/link";
const messages={success:["Pagamento enviado","Estamos aguardando a confirmação segura do Asaas."],cancel:["Pagamento cancelado","A estadia continua aguardando pagamento."],expired:["Link expirado","Volte ao Frentista para escolher outra forma de pagamento."]} as const;
export default async function Page({searchParams}:{searchParams:Promise<{status?:string}>}){const status=(await searchParams).status;const message=messages[status as keyof typeof messages]??messages.success;return <main className="grid min-h-screen place-items-center bg-slate-50 p-6"><section className="w-full max-w-lg rounded-3xl border bg-white p-8 text-center shadow-sm"><p className="text-sm font-bold uppercase tracking-wide text-blue-600">Star Carvalhos</p><h1 className="mt-3 text-3xl font-bold">{message[0]}</h1><p className="mt-3 text-slate-600">{message[1]}</p><p className="mt-2 text-sm text-slate-500">O retorno desta página não confirma pagamento. A confirmação ocorre somente pelo webhook.</p><Link href="/login" className="mt-6 inline-flex rounded-xl bg-blue-600 px-5 py-3 font-bold text-white">Voltar ao sistema</Link></section></main>}
