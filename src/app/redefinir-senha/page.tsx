"use client";
import { useActionState } from "react"; import { reset, type ResetState } from "./actions";
const initial: ResetState = {};
export default function ResetPage() { const [state, action, pending] = useActionState(reset, initial); return <main className="grid min-h-screen place-items-center bg-slate-50 p-4"><form action={action} className="w-full max-w-md space-y-5 rounded-3xl border bg-white p-8 shadow-xl"><h1 className="text-3xl font-bold">Defina uma nova senha</h1><p className="text-sm text-slate-500">Use uma senha exclusiva com pelo menos 8 caracteres.</p><input name="password" type="password" autoComplete="new-password" required minLength={8} placeholder="Nova senha" className="h-12 w-full rounded-xl border px-4" /><input name="confirm" type="password" autoComplete="new-password" required minLength={8} placeholder="Confirme a nova senha" className="h-12 w-full rounded-xl border px-4" />{state.error && <p className="text-sm text-red-600">{state.error}</p>}<button disabled={pending} className="h-12 w-full rounded-xl bg-blue-600 font-bold text-white">Salvar nova senha</button></form></main>; }
